apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.name }}
spec:
  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.ports.api }}
  type: ClusterIP
  selector:
    app: {{ .Values.app.name }}
